---
systemd_service_defaults:
  state: started
  enabled: true
  docker:
    docker_binary: /usr/bin/docker
    pull: true
    use_wrapper: false
    default_tag: latest
    stop_timeout: 2
  restart_on_change: true
